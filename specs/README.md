# Component Specs

Local component contracts for SDKWork Deploy.

- `component.spec.json`: component identity, public exports, and canonical spec links.
- `topology.spec.json`: runtime topology profile vocabulary and surface bindings.

Canonical standards remain in `../sdkwork-specs/`.
