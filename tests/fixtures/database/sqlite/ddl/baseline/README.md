# SQLite Test Baseline

This fixture supports repository-level SQLite tests only. The Deploy authoritative database root
and every gateway, worker, container, and shared environment use PostgreSQL.

Optional full baseline snapshots when `baselineStrategy` is not `migrations-only`.
