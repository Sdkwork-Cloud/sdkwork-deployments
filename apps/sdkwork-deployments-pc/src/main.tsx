import { StrictMode } from "react"; import { createRoot } from "react-dom/client"; import { App } from "./App.tsx"; import { bootstrapDeploymentsRuntime } from "./bootstrap/runtime.ts"; import "./styles.css"; const element = document.getElementById("root"); if (!element) throw new Error("Application root element is missing"); const root = createRoot(element); root.render(<div className="bootstrap-state">SDKWork Deployments</div>); void bootstrapDeploymentsRuntime().then((runtime) => root.render(<StrictMode><App runtime={runtime} /></StrictMode>)).catch((cause) => { console.error(cause); root.render(<div className="fatal-state">Runtime configuration could not be loaded.</div>); });

