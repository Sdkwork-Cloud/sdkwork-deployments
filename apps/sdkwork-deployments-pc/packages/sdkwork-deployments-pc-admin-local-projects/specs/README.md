# Local Projects (Admin)

Browse Docker deploy modules and local runtime nodes through the Drive deploy sandbox (`deploy.local.opt_deploy`). The Admin package receives an injected `SandboxExplorerPort` from the application shell and never imports `@sdkwork/drive-app-sdk`.
