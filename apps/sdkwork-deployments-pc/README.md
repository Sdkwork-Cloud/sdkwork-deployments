# SDKWork Deployments PC

Publishing control-plane application for application packages, releases, deployments, sites, domains, certificates, configuration, monitoring, infrastructure, and audit evidence.

The tenant Console uses the Deploy App SDK. Package bytes are uploaded through the Drive App SDK and then registered in Deploy by stable Drive reference. The lazy backend-admin surface uses the Deploy Backend SDK and exposes only currently authoritative infrastructure and audit operations.

