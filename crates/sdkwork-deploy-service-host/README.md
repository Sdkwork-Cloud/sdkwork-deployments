# SDKWork Deploy Service Host

In-process composition root for Deploy repositories, provider ports, and application services.
HTTP assembly and background workers consume this host and do not construct persistence or SDK
adapters themselves.
