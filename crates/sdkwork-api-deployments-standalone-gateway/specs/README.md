# SDKWork Deploy Standalone Gateway Specs

`component.spec.json` defines the standalone application ingress, API assembly dependency, database
migration entrypoint, runtime configuration, and verification contract.
