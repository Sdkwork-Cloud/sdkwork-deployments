# Deploy Application Service Component

`component.spec.json` declares the Deploy service use cases and its repository, Drive, and Web
runtime ports. Runtime construction belongs to the API Assembly; SQL belongs to the SQLx repository.
