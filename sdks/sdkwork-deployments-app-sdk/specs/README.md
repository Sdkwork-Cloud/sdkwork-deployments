# SDKWork Deploy App SDK Specs

`component.spec.json` defines the App API SDK family ownership, authority, dependency, and
verification contract. Global API and SDK standards remain authoritative through its relative
`canonicalSpecs` links.
