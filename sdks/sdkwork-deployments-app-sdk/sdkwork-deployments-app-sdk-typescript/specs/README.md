# SDKWork Deploy App SDK TypeScript Facade Specs

`component.spec.json` defines the authored TypeScript consumer facade over the generator-owned
Deploy App API transport. The facade exposes only the canonical `@sdkwork/deployments-app-sdk` package.
