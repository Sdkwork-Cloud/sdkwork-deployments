# SDKWork Deploy Backend SDK TypeScript Facade Specs

`component.spec.json` defines the authored TypeScript consumer facade over the generator-owned
Deploy Backend API transport. The facade exposes only the canonical `@sdkwork/deployments-backend-sdk`
package for explicit backend-admin consumers.
