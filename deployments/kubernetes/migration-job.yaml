apiVersion: batch/v1
kind: Job
metadata:
  name: sdkwork-deploy-db-migrate
  labels:
    app.kubernetes.io/name: sdkwork-deploy
    app.kubernetes.io/component: db-migrate
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: sdkwork-deploy
        app.kubernetes.io/component: db-migrate
    spec:
      restartPolicy: OnFailure
      containers:
        - name: migrate
          image: sdkwork-deployments-standalone-gateway:latest
          command: ["sdkwork-deployments-standalone-gateway", "db-migrate"]
          env:
            - name: SDKWORK_DEPLOY_APP_ROOT
              value: /app
            - name: SDKWORK_DEPLOY_ENVIRONMENT
              value: production
            - name: SDKWORK_DEPLOY_DATABASE_AUTO_MIGRATE
              value: "true"
            - name: SDKWORK_DEPLOY_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: sdkwork-deploy-database
                  key: database-url
