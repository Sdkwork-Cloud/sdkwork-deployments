apiVersion: v1
kind: Service
metadata:
  name: sdkwork-deploy-api-server
  labels:
    app.kubernetes.io/name: sdkwork-deploy
    app.kubernetes.io/component: api-server
spec:
  selector:
    app.kubernetes.io/name: sdkwork-deploy
    app.kubernetes.io/component: api-server
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
